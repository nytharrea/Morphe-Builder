"""Incremental builder helpers for Builder-Morphe.

This package is intentionally dependency-light: it uses pydantic (already a
transitive dependency via pydantic-settings) and the standard library only.
"""
