name: Patch App

on:
  workflow_dispatch:

concurrency:
  group: patch-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      contents: write

    outputs:
      release_tag: ${{ steps.release.outputs.tag }}
      release_name: ${{ steps.release.outputs.name }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.13"

      - name: Install & freeze Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade -r requirements.txt
          pip freeze > requirements-lock.txt

      - name: Commit updated lockfile
        run: |
          if [ -n "$(git status --porcelain -- requirements-lock.txt)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add requirements-lock.txt
            git commit -m "chore: update requirements-lock.txt [skip ci]"
            git push
          else
            echo "requirements-lock.txt unchanged, skipping commit."
          fi

      - name: Compute release tag
        id: release
        run: python prepare_release.py

  patch:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 25

    permissions:
      contents: write
      actions: write
      id-token: write
      attestations: write
      artifact-metadata: write

    strategy:
      fail-fast: false
      max-parallel: 30
      matrix:
        app:
          - youtube
          - youtube-music
          - reddit
          - reddit-adobo
          - twitter
          - twitter-x
          - instagram
          - gboard
          - speedtest
          - brave
          - proton-vpn
          - tiktok
          - tiktok-hxreborn
          - warp
          - inshot
          - google-photos
          - inure-github
          - inure-play
          - proton-pass
          - notesnook

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          persist-credentials: true

      - name: Show runner IP
        run: curl -s --max-time 10 https://api.ipify.org || true

      - name: Jitter delay before browser starts
        run: |
          delay=$(( RANDOM % 25 ))
          echo "Sleeping ${delay}s to avoid every matrix job hitting APKMirror in the same instant"
          sleep "$delay"

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.13"

      - name: Setup Java
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6
        with:
          distribution: temurin
          java-version: 25

      - name: Install Firefox runtime libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-0 libx11-xcb1 libasound2t64 || sudo apt-get install -y libgtk-3-0 libx11-xcb1 libasound2

      - name: Install Python dependencies (from lockfile, pinned versions)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-lock.txt

      - name: Fetch Camoufox browser binary
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ok=0
          for i in 1 2 3; do
            python3 -m camoufox fetch 2>&1 | python3 -c '
          import re
          import sys

          pattern = re.compile(r"^(?P<prefix>.*?)\s*:?\s*(?P<pct>[0-9]{1,3})%\s*$")
          pending = None
          pending_key = None

          def flush():
              global pending
              if pending is not None:
                  print(pending, flush=True)
                  pending = None

          def handle(text):
              global pending, pending_key
              match = pattern.match(text)
              if match:
                  key = match.group("prefix")
                  if key != pending_key:
                      flush()
                  pending = text
                  pending_key = key
              else:
                  flush()
                  print(text, flush=True)

          buf = ""
          while True:
              chunk = sys.stdin.read(4096)
              if not chunk:
                  break
              buf += chunk
              while True:
                  m = re.search(r"[\r\n]", buf)
                  if not m:
                      break
                  line, buf = buf[: m.start()], buf[m.end() :]
                  if line:
                      handle(line)
          if buf:
              handle(buf)
          flush()
          ' || true

            if python3 -c '
          import asyncio
          from camoufox.async_api import AsyncCamoufox

          async def main():
              async with AsyncCamoufox(headless=True) as browser:
                  page = await browser.new_page()
                  await page.close()

          asyncio.run(main())
          '; then
              ok=1
              break
            fi

            echo "Camoufox smoke-test launch failed on attempt $i - removing cache and retrying fetch..."
            python3 -m camoufox remove -y || true
            delay=$(( 5 + RANDOM % 10 ))
            sleep "$delay"
          done

          if [ "$ok" -ne 1 ]; then
            echo "Camoufox browser could not be launched after 3 fetch+smoke-test attempts" >&2
            exit 1
          fi
          echo "Camoufox binary confirmed working at $(python3 -m camoufox path || true)"

      - name: Setup Keystore
        run: |
          set -euo pipefail
          rm -f Panemi.keystore
          umask 077
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > Panemi.keystore
          chmod 600 Panemi.keystore

      - name: Run script
        run: python -u main.py
        env:
          PYTHONUNBUFFERED: "1"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          TARGET_APP: ${{ matrix.app }}
          KS_PATH: "Panemi.keystore"
          KS_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KS_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          REQUIRE_CUSTOM_KEYSTORE: "true"
          VERSION_POLICY: "max_patches_then_version"

      - name: Upload patched APK artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: apk-${{ matrix.app }}
          path: dist/*
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload diagnostic screenshots
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: diagnostics-${{ matrix.app }}-${{ github.run_id }}
          path: diagnostics/
          if-no-files-found: ignore
          retention-days: 7

      - name: Commit updated signature files
        if: always()
        run: python commit_signature.py "${{ matrix.app }}"

      - name: Generate build provenance attestation
        if: ${{ hashFiles('dist/*.apk') != '' }}
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6
        with:
          subject-path: |
            dist/*.apk
            !dist/microg*.apk

  finalize:
    needs: [prepare, patch]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Download patched APK artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: apk-*
          path: artifacts
          merge-multiple: true

      - name: Create release and clean up old releases
        run: python finalize_release.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          RELEASE_TAG: ${{ needs.prepare.outputs.release_tag }}
          RELEASE_NAME: ${{ needs.prepare.outputs.release_name }}
          ARTIFACTS_DIR: artifacts
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          APPRISE_URLS: ${{ secrets.APPRISE_URLS }}
          RELEASE_KEEP_LATEST: "3"

  cleanup:
    needs: finalize
    if: always()
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@b3018382ca039b53d238908238bd35d1fb14f8ee
        with:
          retain_days: 0
          keep_minimum_runs: 1
