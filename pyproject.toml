[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "builder-morphe"
version = "2.0.0"
description = "FlareSolverr ve APKMirror destekli Android yama boru hattı"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "urllib3>=2.0.0"
]
